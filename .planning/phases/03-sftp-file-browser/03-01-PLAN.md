---
phase: 03-sftp-file-browser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MobaAlt/Models/SFTPItem.swift
  - MobaAlt/Models/TransferTask.swift
  - MobaAlt/Utilities/SFTPPanelPosition.swift
  - MobaAlt/Services/SFTPBrowserService.swift
  - MobaAltTests/SFTPBrowserServiceTests.swift
  - MobaAltTests/SFTPFileTransferTests.swift
autonomous: true
requirements:
  - SFTP-01
  - SFTP-02
  - SFTP-03
  - SFTP-04
  - SFTP-05

must_haves:
  truths:
    - "SFTPItem value type holds name, path, size, modificationDate, isDirectory"
    - "TransferTask value type tracks id, remotePath, localURL, direction, progress, status"
    - "SFTPPanelPosition enum has cases: left, right, bottom, hidden; rawValue is String"
    - "SFTPChannel protocol defines: connect(), listDirectory(path:), upload(localURL:toRemotePath:), download(remotePath:toLocalURL:), createDirectory(at:), rename(from:to:), delete(at:), disconnect()"
    - "MockSFTPChannel implements SFTPChannel with controllable stub responses for all methods"
    - "SFTPBrowserService skeleton compiles with SFTPChannel injection point"
    - "SFTPBrowserServiceTests.swift has stub test cases for SFTP-01 through SFTP-05 (all compile, some pending)"
    - "SFTPFileTransferTests.swift has stub test cases for upload/download progress tracking"
  artifacts:
    - path: "MobaAlt/Models/SFTPItem.swift"
      provides: "Remote file/directory metadata value type"
      exports: ["SFTPItem"]
    - path: "MobaAlt/Models/TransferTask.swift"
      provides: "In-progress or completed transfer state"
      exports: ["TransferTask", "TransferDirection", "TransferStatus", "TransferProgress"]
    - path: "MobaAlt/Utilities/SFTPPanelPosition.swift"
      provides: "Panel position enum used by TabItem and Preferences"
      exports: ["SFTPPanelPosition"]
    - path: "MobaAlt/Services/SFTPBrowserService.swift"
      provides: "SFTPChannel protocol, MockSFTPChannel, SFTPBrowserService skeleton"
      exports: ["SFTPChannel", "MockSFTPChannel", "SFTPBrowserService"]
    - path: "MobaAltTests/SFTPBrowserServiceTests.swift"
      provides: "Unit test stubs for all SFTP-01..SFTP-05 requirements"
    - path: "MobaAltTests/SFTPFileTransferTests.swift"
      provides: "Unit test stubs for upload/download progress tracking"
  key_links:
    - from: "MobaAlt/Services/SFTPBrowserService.swift"
      to: "MobaAlt/Models/SFTPItem.swift"
      via: "items: [SFTPItem] property"
      pattern: "\\[SFTPItem\\]"
    - from: "MobaAltTests/SFTPBrowserServiceTests.swift"
      to: "MobaAlt/Services/SFTPBrowserService.swift"
      via: "MockSFTPChannel injection in test setUp"
      pattern: "MockSFTPChannel"
---

<objective>
Define all data contracts and testability infrastructure for Phase 3 before any implementation begins.

Purpose: Establishing protocols and test stubs first (Wave 0 Nyquist compliance) prevents scavenger-hunt problems in later plans — every downstream plan receives the exact types and interfaces it needs. MockSFTPChannel enables pure unit tests without a live SSH connection.
Output: Six files — four production files (models + protocol + skeleton service) and two test stub files — all registered in the Xcode project.
</objective>

<execution_context>
@/Users/itsik.tsarfati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itsik.tsarfati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sftp-file-browser/03-CONTEXT.md
@.planning/phases/03-sftp-file-browser/03-RESEARCH.md

<interfaces>
<!-- Key existing interfaces this plan must conform to -->

From MobaAlt/Services/TabManager.swift:
```swift
struct TabItem: Identifiable {
    let id: UUID
    let sessionId: UUID
    var displayName: String
    let connection: SSHConnection
}
```

From MobaAlt/Utilities/SSHArgumentBuilder.swift:
```swift
static func controlPath(for sessionId: UUID) -> String
// Returns "/tmp/mobaalt-{8hex}.sock"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data models — SFTPItem, TransferTask, SFTPPanelPosition</name>
  <files>
    MobaAlt/Models/SFTPItem.swift,
    MobaAlt/Models/TransferTask.swift,
    MobaAlt/Utilities/SFTPPanelPosition.swift
  </files>
  <behavior>
    - SFTPItem: struct with name: String, path: String, size: Int64, modificationDate: Date, isDirectory: Bool, permissions: String. Equatable, Hashable, Sendable.
    - TransferDirection: enum { upload, download }
    - TransferStatus: enum { pending, inProgress(fractionCompleted: Double), completed, failed(Error) }
    - TransferProgress: struct with bytesTransferred: Int64, totalBytes: Int64, fractionCompleted: Double
    - TransferTask: struct with id: UUID, remotePath: String, localURL: URL, direction: TransferDirection, status: TransferStatus. Identifiable.
    - SFTPPanelPosition: enum with cases left, right, bottom, hidden. RawRepresentable String, CaseIterable, Sendable.
    - Test: SFTPItem created with known values has correct isDirectory flag
    - Test: TransferProgress.fractionCompleted returns bytesTransferred / totalBytes
    - Test: SFTPPanelPosition RawRepresentable roundtrip ("left" -> .left -> "left")
  </behavior>
  <action>
    Create three files with the value types above. No dependencies on Phase 3 services — pure value types only.

    SFTPItem.swift: struct SFTPItem with properties as specified. Conform to Identifiable using path as id (String). Add a computed var isSymlink: Bool = false (placeholder for future extension).

    TransferTask.swift: TransferDirection enum, TransferStatus enum (note: failed case has associated Error — make TransferStatus non-Equatable to avoid Error comparison issues; instead add isCompleted: Bool computed var). TransferProgress struct (bytesTransferred, totalBytes, fractionCompleted computed as Double(bytesTransferred)/Double(max(1,totalBytes))). TransferTask struct with Identifiable.

    SFTPPanelPosition.swift: enum SFTPPanelPosition: String, CaseIterable, Sendable { case left, right, bottom, hidden }. Add var displayName: String { ... } for Preferences UI.

    Register all three files in MobaAlt.xcodeproj via xcodegen or direct pbxproj edit. Add to the Models/ and Utilities/ groups respectively and add to the MobaAlt target Sources build phase.
  </action>
  <verify>
    <automated>cd /Users/itsik.tsarfati/Projects/moba_mac_alternative && xcodebuild build -project MobaAlt.xcodeproj -scheme MobaAlt -destination 'platform=macOS' 2>&1 | grep -E "error:|Build succeeded"</automated>
  </verify>
  <done>Build succeeds with all three files. SFTPItem, TransferTask family, and SFTPPanelPosition are exported from the MobaAlt module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SFTPChannel protocol, MockSFTPChannel, SFTPBrowserService skeleton, and test stubs</name>
  <files>
    MobaAlt/Services/SFTPBrowserService.swift,
    MobaAltTests/SFTPBrowserServiceTests.swift,
    MobaAltTests/SFTPFileTransferTests.swift
  </files>
  <behavior>
    - SFTPChannel protocol: async throwing methods: connect(), disconnect(), listDirectory(path: String) -> [SFTPItem], createDirectory(at path: String), rename(from: String, to: String), delete(at path: String). Non-throwing: send(_ command: String). Marked @MainActor-compatible (no actor requirement on protocol itself).
    - MockSFTPChannel: final class implementing SFTPChannel. Has controllable properties: var mockItems: [SFTPItem] = [], var shouldThrowOnConnect: Bool = false, var didConnect: Bool = false, var lastCommand: String?. connect() sets didConnect = true (throws if shouldThrowOnConnect). listDirectory returns mockItems.
    - SFTPBrowserService: @Observable @MainActor final class. Properties: sessionId: UUID, currentPath: String = "~", items: [SFTPItem] = [], transfers: [TransferTask] = [], isLoading: Bool = false, isConnected: Bool = false. Init takes channel: SFTPChannel (for testability) and sessionId: UUID. Production convenience init takes only sessionId and creates real SFTPSubprocessChannel internally (Plan 03-02 will implement the real channel). Stub methods: connect() async throws, listDirectory(path:) async throws, upload(localURL:toRemotePath:) async throws, download(remotePath:toLocalURL:) async throws, createDirectory(at:) async throws, rename(from:to:) async throws, delete(at:) async throws — all bodies are `fatalError("Not yet implemented")` EXCEPT connect() and listDirectory() which use the injected channel.
    - SFTPBrowserServiceTests: import Testing; test stubs using MockSFTPChannel. Include: testConnectSetsIsConnected(), testListDirectoryPopulatesItems(), testConnectFailurePropagatesError(), testCreateDirectoryCalled(), testRenameCalled(), testDeleteCalled(). Mark incomplete tests @Test with a comment "// TODO: implement in 03-02".
    - SFTPFileTransferTests: import Testing; stubs: testUploadProgressTracking(), testDownloadProgressTracking(), testMultipleSimultaneousTransfers(). All stubs compile but call Issue.record("not implemented") and return.
  </behavior>
  <action>
    Create SFTPBrowserService.swift in MobaAlt/Services/. Define SFTPChannel protocol first, then MockSFTPChannel (can live in the same file inside #if DEBUG or in a separate TestSupport file — keep MockSFTPChannel in the main module so tests can import it without special flags; mark it internal). Define SFTPBrowserService class with the injectable channel.

    Important: The production init (convenience init(sessionId:)) must not reference SFTPSubprocessChannel yet — that class does not exist until Plan 03-02. Use a placeholder: the convenience init calls self.init(channel: MockSFTPChannel(), sessionId: sessionId) with a comment "// TODO: 03-02 replaces MockSFTPChannel with SFTPSubprocessChannel".

    Create SFTPBrowserServiceTests.swift in MobaAltTests/. The two already-implementable tests are:
    - testConnectSetsIsConnected: create MockSFTPChannel, create SFTPBrowserService(channel: mock, sessionId: UUID()), call await service.connect(), #expect(service.isConnected == true)
    - testListDirectoryPopulatesItems: set mock.mockItems = [SFTPItem(...)], call await service.listDirectory(path: "~"), #expect(service.items.count == 1)

    Create SFTPFileTransferTests.swift with compilable stub @Test functions that call Issue.record("not implemented") and return — satisfies Nyquist Wave 0 requirement.

    Register both new production and test files in Xcode project.
  </action>
  <verify>
    <automated>cd /Users/itsik.tsarfati/Projects/moba_mac_alternative && xcodebuild test -project MobaAlt.xcodeproj -scheme MobaAlt -destination 'platform=macOS' -only-testing:MobaAltTests/SFTPBrowserServiceTests 2>&1 | tail -10</automated>
  </verify>
  <done>xcodebuild test runs SFTPBrowserServiceTests. testConnectSetsIsConnected and testListDirectoryPopulatesItems pass. Stub tests either pass or are marked Issue.record. No compilation errors in any file.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
`cd /Users/itsik.tsarfati/Projects/moba_mac_alternative && xcodebuild test -project MobaAlt.xcodeproj -scheme MobaAlt -destination 'platform=macOS' 2>&1 | tail -20`

Confirm new files appear in xcode project:
`grep -c "SFTPBrowserService\|SFTPItem\|TransferTask\|SFTPPanelPosition" /Users/itsik.tsarfati/Projects/moba_mac_alternative/MobaAlt.xcodeproj/project.pbxproj`
(should return >= 6, one entry per file per build phase reference)
</verification>

<success_criteria>
- All six new files compile without errors
- SFTPBrowserServiceTests runs (2 real tests pass, stubs are no-ops)
- Full test suite remains green (no regressions from Phase 2)
- SFTPChannel protocol, MockSFTPChannel, and SFTPBrowserService are importable from test target
</success_criteria>

<output>
After completion, create `.planning/phases/03-sftp-file-browser/03-01-SUMMARY.md`
</output>
