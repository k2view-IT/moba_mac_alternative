---
phase: 03-sftp-file-browser
plan: 06
type: execute
wave: 5
depends_on:
  - 03-05
files_modified: []
autonomous: false
requirements:
  - SFTP-01
  - SFTP-02
  - SFTP-03
  - SFTP-04
  - SFTP-05

must_haves:
  truths:
    - "SFTP panel appears automatically on the left when an SSH session connects"
    - "User can navigate the remote directory tree by double-clicking folders and clicking breadcrumb segments"
    - "User can upload files by dragging from Finder into the SFTP panel"
    - "User can download files by dragging from the SFTP panel to Finder"
    - "User can create, rename, and delete files and folders via context menu"
    - "No separate SSH authentication is required to open the SFTP panel (ControlMaster reuse)"
    - "SFTP panel position can be changed per-tab via toolbar button and globally via Preferences"
  artifacts:
    - path: "MobaAlt/Services/SFTPBrowserService.swift"
      provides: "Fully working SFTP service"
    - path: "MobaAlt/Views/SFTP/SFTPPanelView.swift"
      provides: "Rendered SFTP panel alongside terminal"
    - path: "MobaAlt/Views/SFTP/SFTPFileListView.swift"
      provides: "Interactive file list with all operations"
  key_links:
    - from: "Human verifier"
      to: "Running SSH session"
      via: "Manual end-to-end workflow"
      pattern: "n/a"
---

<objective>
Human verification of the complete SFTP file browser through a live SSH session.

Purpose: All automated tests use MockSFTPChannel and cannot verify real /usr/bin/sftp subprocess behavior, ControlMaster socket reuse, drag-and-drop with Finder, or the visual layout. This checkpoint confirms the full SFTP feature works end-to-end with a real server.
Output: Human approval or list of issues for gap closure.
</objective>

<execution_context>
@/Users/itsik.tsarfati/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itsik.tsarfati/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sftp-file-browser/03-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>End-to-end SFTP verification with live SSH session</name>
  <action>Human performs the 18-step verification workflow below. No automated action — this is a human-only verification step.</action>
  <what-built>
    Complete SFTP file browser panel — auto-opens alongside SSH terminal sessions, browses remote directories with breadcrumb navigation and sortable columns, uploads via Finder drag-drop or toolbar button, downloads via panel drag-to-Finder or right-click menu, and supports create/rename/delete via context menu. No new authentication required (reuses Phase 2 ControlMaster socket).
  </what-built>
  <how-to-verify>
    Prerequisites: Have an SSH server accessible (can be localhost with SSH enabled: System Settings > Sharing > Remote Login).

    1. Launch MobaAlt app.
    2. Open an SSH session to a server you have access to.
    3. Verify: SFTP panel appears automatically on the LEFT side within ~3 seconds of connecting. Terminal is on the right. No additional password prompt appeared for SFTP.
    4. Verify: Breadcrumb at top shows the home directory path (e.g., "~ / home / user").
    5. Double-click a folder in the list — verify panel navigates into it and breadcrumb updates.
    6. Click a breadcrumb segment — verify panel navigates back to that path.
    7. Press Cmd+Shift+. — verify hidden dotfiles appear/disappear in the listing.
    8. Click the "Name" column header — verify A-Z/Z-A sort toggle. Verify folders appear before files.
    9. Click the SFTP toolbar button (top right) — verify panel cycles through Left / Right / Bottom / Hidden positions.
    10. Right-click a file — verify context menu shows: New File, New Folder, Rename, Delete, Download, Copy Path.
    11. Click "New Folder" — verify a new "NewFolder" appears in the listing.
    12. Right-click "NewFolder" — Rename — verify inline TextField activates. Type a new name, press Enter — verify name updated on remote.
    13. Right-click the renamed folder — Delete — verify it disappears from listing.
    14. Drag a file from Finder/Desktop into the SFTP panel — verify upload progress appears at the bottom of the panel, then the file appears in the listing.
    15. Drag a file from the SFTP panel to the Desktop — verify file downloads to Desktop.
    16. Right-click a file — Download — NSSavePanel appears — choose location — verify file downloads there.
    17. Double-click a file (e.g., a .txt or .png) — verify it downloads to /tmp/mobaalt-downloads/ and opens in the default app (TextEdit/Preview).
    18. Open Preferences (Cmd+,) — find the "SFTP Panel" section — change Default Panel Position to "Right" — close Preferences — open a new SSH tab — verify SFTP panel appears on the right.
  </how-to-verify>
  <verify>
    <automated>MISSING — this plan is a human checkpoint; no automated command applies. Automated suite already passed in Plan 03-05.</automated>
  </verify>
  <done>All 18 verification steps pass. Human types "approved".</done>
  <resume-signal>Type "approved" if all 18 steps pass, or describe any failures in detail (include step number and observed vs expected behavior) for gap closure.</resume-signal>
</task>

</tasks>

<verification>
This plan's verification IS the human checkpoint above.
</verification>

<success_criteria>
- All 18 verification steps pass without errors
- SFTP panel auto-opens on SSH connect without re-authentication
- All upload/download paths work (drag, toolbar, context menu)
- All file operations work (create folder, rename, delete)
- Panel position is configurable per-tab and globally
- Phase 3 is complete and ready for ROADMAP status update
</success_criteria>

<output>
After approval, create `.planning/phases/03-sftp-file-browser/03-06-SUMMARY.md` and update ROADMAP.md Phase 3 status to complete.
</output>
