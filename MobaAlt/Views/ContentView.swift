import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("MobaAlt")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

// Note: #Preview macro requires Xcode's plugin infrastructure; omitted for CLI compilation.
